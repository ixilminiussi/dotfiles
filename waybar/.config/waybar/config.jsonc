{
  "position": "top",
  "modules-left": ["power-profiles-daemon", "cpu", "memory", "temperature", "battery", "hyprland/workspaces"],
  "modules-center": ["hyprland/window"],
  "modules-right": ["pulseaudio", "pulseaudio_mic", "backlight", "bluetooth", "network", "clock"],

  "power-profiles-daemon": {
    "format": "{icon}",
    "tooltip-format": "Power profile: {profile}\nDriver: {driver}",
    "tooltip": true,
    "format-icons": {
      "default": " ",
      "performance": " ",
      "balanced": " ",
      "power-saver": " "
    }
  },

  "cpu": {
    "format": "  {usage}%",
  },
    
  "memory": {
    "format": "  {used:0.1f}GiB"
  },

  "temperature": {
    "format": "{icon} {temperatureC}°C",
    "hwmon-path": "/sys/class/hwmon/hwmon2/temp1_input",
    "format-icons": ["", "", "", "", ""],
    "critical-threshold": 80,
  },

  "clock": {
    "format": "󱑀  {:%I:%M %p}",
    "format-alt": "󰃰  {:%d %b %I:%M %p}",
    "tooltip-format": "{calendar}",
    "calendar": {
      "mode": "month",
      "format": {
        "months": "<span color='#b4befe'>{}</span>",
        "days": "<span color='#cdd6fa'>{}</span>",
        "weekdays": "<span color='#f9e2af'>{}</span>",
        "today": "<span color='#f38ba8'><b>{}</b></span>"
      }
    }
  },

  "hyprland/workspaces": {
    "format": "{icon}",
    "format-icons": {
      "active": "",   
      "default": ""  
    },
    "persistent-workspaces": {
      "*": 4
    },
    "active-only": false
  },

  "hyprland/window": {
    "format": "{}",
    "max-length": 20,
    "tooltip": false
  },
 
  "battery": {
    "states": {
      "warning": 30,
      "critical": 15
    },
    "format": "{icon} {capacity}%",
    "format-warning": "{icon} {capacity}%",
    "format-critical": "{icon} {capacity}%",
    "format-charging": "  {capacity}%",
    "format-plugged": "  {capacity}%",
    "format-alt": "{icon} {time}",
    "format-full": "  {capacity}%",
    "format-icons": [" ", " ", " ", " ", " "],
    "tooltip-format": "{time}"
  },

  "network": {
    "format-alt": "{icon} {signalStrength}",
    "format-wifi": "{icon} {essid}",
    "format-icons": ["󰤯 ", "󰤟 ", "󰤢 ", "󰤨 "], // low, medium, high
    "format-ethernet": "󰈀  Connected",
    "format-disconnected": "  Disconnected",
    "tooltip-format-wifi": "{essid}"
  },

  "bluetooth": {
    "on-click": "bluetoothctl power toggle",
    "on-click-right": "bluetoothctl devices | grep Connected",
  },

  "pulseaudio": {
    "format": "{icon} {volume}",
    "format-muted": "",
    "scroll-step": 5,
    "format-icons": {
      "default": [" ", " ", " "]
    },
    "on-click": "wpctl set-mute @DEFAULT_SINK@ toggle",
    "on-scroll-up": "wpctl set-volume @DEFAULT_SINK@ 5%- --limit 1.5",
    "on-scroll-down": "wpctl set-volume @DEFAULT_SINK@ 5%+ --limit 1.5",
  },

  "pulseaudio_mic": {
    "format": "{icon} {volume}",
    "format-muted": "",
    "scroll-step": 5,
    "format-icons": {
      "default": [" ", " ", " "]
    },
    "source": true,
    "on-click": "wpctl set-mute @DEFAULT_SOURCE@ toggle",
    "on-scroll-up": "wpctl set-volume @DEFAULT_SOURCE@ 5%- --limit 1.0",
    "on-scroll-down": "wpctl set-volume @DEFAULT_SOURCE@ 5%+ --limit 1.0",
  },

  "backlight": {
    "device": "intel_backlight",
    "format": "{icon} {percent}",
    "format-icons": ["󰃞 "],
  },

  "custom/caway": {
    "format": "{text}",
    "format-icons": {
      "Paused": "\uf04c ",
      "Stopped": "\uf04d "
    },
    "escape": true,
    "tooltip": true,
    "exec": "sh /home/ixilminiussi/.config/waybar/scripts/caway.sh -b 12 -f 60",
    "return-type": "json",
    "on-click": "playerctl play-pause",
    "on-scroll-up": "playerctl previous",
    "on-scroll-down": "playerctl next",
    "on-click-right": "g4music",
    "max-length": 35
  },

  "custom/power": {
    "format": "{icon}",
    "tooltip": false,
    "on-click": "wlogout --protocol layer-shell"
  }
}
